# Build62.1 Release Notes
**Grant Park Events - Bug Fix Release**  
**Version:** v2.3.0-build62.1  
**Date:** January 30, 2026  
**Build Type:** Bug Fix - Environment Variable Migration

---

## 🐛 Bug Fix

### **Issue:**
Build62 had Google Indexing API credential issues - `GSC_CREDENTIALS` JSON parsing errors in Netlify environment variables.

### **Solution:**
Migrated from single JSON credential to split environment variables.

### **Changes:**
- Updated `gsc-request-index.js` to use split env vars
- Updated `gsc-midnight-batch.js` to use split env vars
- Created .env file upload method for Netlify

### **New Environment Variables:**
- `GSC_PROJECT_ID`
- `GSC_PRIVATE_KEY_ID`
- `GSC_PRIVATE_KEY`
- `GSC_CLIENT_EMAIL`

---

## ⚠️ Known Issues

- `gsc-index-status.js` still checking for old `GSC_CREDENTIALS` variable
- Fixed in Build62.2

---

## 📋 Deployment

1. Upload .env file to Netlify with 4 split variables
2. Deploy Build62.1
3. Test Google API functions

---

## ✅ Status

Superseded by Build62.2 (which completes the credential migration).
